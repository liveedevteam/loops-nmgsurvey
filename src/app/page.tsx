/**
 * Home Page
 * 
 * Main entry point for the survey application.
 * Renders the SurveyContainer which handles the entire survey flow.
 */

import { SurveyContainer } from '@/components/survey';

export default function HomePage() {
  return <SurveyContainer />;
}
